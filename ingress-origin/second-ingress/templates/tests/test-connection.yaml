apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "second-ingress.fullname" . }}-test-connection"
  labels:
    {{- include "second-ingress.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "second-ingress.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
